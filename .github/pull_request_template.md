## What changes

<!-- One or two sentences. The PR title must follow Conventional Commits —
     release-please builds the version and changelog from it. -->

## How it was checked

<!-- Which device and firmware, what you ran. For protocol changes please attach
     the relevant part of `adb logcat -s M2SYNC`. -->

- [ ] built (`./gradlew :app:assembleDebug`)
- [ ] tried against a real bike computer
- [ ] rides still land in Health Connect (`VERIFY` reports matching numbers)
